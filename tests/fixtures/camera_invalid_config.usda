#usda 1.0

def Xform "Target"
{
}

def Camera "Camera" (
    prepend apiSchemas = ["RunnerCameraRigAPI"]
)
{
    uniform double runner:camera:distance = -1
    uniform token runner:camera:mode = "thirdPerson"
    rel runner:camera:target = </Target>
}
